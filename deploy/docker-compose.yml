services:
  countdown:
    image: ghcr.io/anomalyco/countdown:latest
    container_name: countdown
    restart: unless-stopped
    ports:
      - "3000"
    environment:
      - NODE_ENV=production
    networks:
      - proxy
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

networks:
  proxy:
    external: true
